<Window x:Class="DrawingApp.CreateRoom"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Create Room Options" Height="600" Width="900"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft">

    <Border Background="#FDF5E6" CornerRadius="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Direction="0" ShadowDepth="0" Color="#D2B48C" Opacity="0.4"/>
        </Border.Effect>

        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Button Content="✕" Click="ExitButtonClick" 
                        Background="Transparent" BorderThickness="0" Foreground="#8B7355" 
                        FontSize="22" Cursor="Hand" VerticalAlignment="Center" HorizontalAlignment="Left"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="שלום, " FontSize="22" FontWeight="SemiBold" Foreground="#966859" VerticalAlignment="Center"/>
                    <TextBlock x:Name="UserNameLabel" Text="אורח" FontSize="22" FontWeight="Bold" Foreground="#966859" VerticalAlignment="Center"/>

                    <Border Width="45" Height="45" Background="#966859" CornerRadius="22.5" Margin="15,0,0,0">
                        <TextBlock Text="👤" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="20"/>
                    </Border>
                </StackPanel>
            </Grid>

            <UniformGrid Grid.Row="1" Columns="2" Margin="0,40,0,0">

                <Button Margin="20" Cursor="Hand" Click="NewProjectClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" CornerRadius="25" BorderThickness="0" ClipToBounds="True">
                                <Border.Background>
                                    <ImageBrush ImageSource="/Images/newPaint.jpg" Stretch="UniformToFill" />
                                </Border.Background>
                                <Grid Background="#88000000">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="➕" FontSize="60" HorizontalAlignment="Center" Margin="0,0,0,10" Foreground="White"/>
                                        <TextBlock Text="פרויקט חדש" FontSize="26" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                        <TextBlock Text="התחל דף חלק ונקי" FontSize="15" Foreground="#EEE" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Margin="20" Cursor="Hand" Click="UploadAssetsClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" CornerRadius="25" BorderThickness="0" ClipToBounds="True">
                                <Border.Background>
                                    <ImageBrush ImageSource="/Images/uploadPaint.jpg" Stretch="UniformToFill" />
                                </Border.Background>
                                <Grid Background="#88000000">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="⭡" FontSize="60" HorizontalAlignment="Center" Margin="0,0,0,10" Foreground="White"/>
                                        <TextBlock Text="העלאת קבצים" FontSize="26" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                        <TextBlock Text="ייבא תמונה או סקיצה קיימת" FontSize="15" Foreground="#DDD" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </UniformGrid>
        </Grid>
    </Border>
</Window>